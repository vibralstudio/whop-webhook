[phases.setup]
nixPkgs = ["...", "python3", "python3Packages.pip", "pango", "cairo", "gdk-pixbuf", "fontconfig"]

[phases.install]
cmds = ["...", "pip3 install -r requirements.txt --break-system-packages || pip3 install -r requirements.txt"]
